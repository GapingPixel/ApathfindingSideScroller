/// @description scr_enemy_contact_damage(damage)

///Contact Damage
function scr_enemy_contact_damage(damage) {
	/*
	arg0 = plyr 
	arg1 = damage
	*/
	var dmg = damage;


	if dmg > 0
	if enemy_hp > 0 && enemy_state = enemyState.NORMAL
	{
	    with(other)
	        plyr_damage(char_id,dmg)
	}



}
