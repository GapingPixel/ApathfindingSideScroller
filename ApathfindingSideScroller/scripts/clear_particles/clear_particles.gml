/// @description /Script to clear out particles
function clear_particles() {
	part_particles_clear(global.parsys);
	part_particles_clear(global.parsys_below);
	part_particles_clear(global.parsys_high);
	part_particles_clear(global.parsys_deep);



}
