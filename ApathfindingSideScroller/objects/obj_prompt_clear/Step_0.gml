/// @description Prompt Pos
x = global.cam_x + (global.cam_width/2);
y = global.cam_y + (global.cam_height/2);

/*switch(prompt_var_clear_behavior)
{
    case 1://In
	if image_xscale < 0.5
        image_xscale += 0.1;
	break;
	
	case 2://Out
	if image_yscale > 0.5
        image_yscale -= 0.1;
	else
		image_yscale = 0;
	break;
}*/

