/// @description Animation
x = global.cam_x + (global.cam_width/2);
y = global.cam_y + (global.cam_height/4);

    
if image_yscale > 0.5
{
    image_yscale -= 0.05;
   
}

if image_xscale > 0.5
{
    image_xscale -= 0.05;
}

